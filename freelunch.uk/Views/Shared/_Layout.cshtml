@{ 
    
    ViewBag.twitter = "@meetfreelunch";

    string _username = @User.Identity.GetUserName();
    string _userid = @User.Identity.GetUserId();
}

@using Microsoft.AspNet.Identity
@using freelunch.uk.Common

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - freelunch.uk</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/freelunch")

</head>
<body>
    <script type="text/javascript">
    $(function () {
        if (document.cookie.indexOf("cookies") < 0) {
            $('#cookie-consent').slideDown('slow');
        }
        else {
            $('#cookie-consent');
        }

        $('#consent').on('click', function () {
            document.cookie = "cookies=yes; max-age=" + (5 * 365 * 24 * 60 * 60);
            $('#cookie-consent').slideUp('slow');
        });
        $('#learn-more').on('click', function () {
            location.href = '@Url.Action("Privacy", "Home")';
        })
    });
</script>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div id="cookie-consent" class="cookie-consent">
                This site uses cookies to personalise content and adverts, to provide social media features and to analyse traffic.
                <button class="btn btn-xs btn-white" id="consent">I'm happy with this</button> <button class="btn btn-xs btn-white" id="learn-more">Learn more</button>
            </div>
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("freelunch.uk", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>
                        <a href="@Url.Action("Specialist", "Home")">
                            <i class="fa fa-user" aria-hidden="true"></i> Specialist
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Lunch", "Home")">
                            <i class="fa fa-cutlery" aria-hidden="true"></i> Lunch
                        </a>
                    </li>
                </ul>

                @if (Request.IsAuthenticated)
                {
                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            <a href="" data-toggle="modal" data-target="#editTarget">
                                <span class="glyphicon glyphicon-cog" aria-hidden="true" style="font-size: 18px" title="Account: @_username"></span>
                            </a>
                        </li>
                    </ul>
                }
                else
                {
                    <ul class="nav navbar-nav navbar-right">
                        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
                        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
                    </ul>
                }

            </div>
        </div>
    </div>

    <div id="editTarget" class="modal fade" role="dialog" tabindex='-1'>
        <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close close-button" data-dismiss="modal">
                        <span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span>
                    </button>
                    <h4 class="modal-title">User Preferences</h4>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-md-8">
                            @Html.ActionLink("Hello " + _username, "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @style = "text-decoration: none" })
                        </div>
                        <div class="col-md-4">
                            @Html.ActionLink("Privacy", "Privacy", "Home")
                        </div>
                    </div>
                    <div class="row">
                        @Html.Partial("_SpecialistLink")
                    </div>
                </div>
                <div class="modal-footer">
                    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                    {
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-default">
                            <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> Log off
                        </button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">
                            <span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span> Cancel
                        </button>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>
                Contact us at: <i class="fa fa-envelope" aria-hidden="true"></i><a href="mailto:info@freelunch.uk" style="text-decoration: none" title="Send us an email"> info@freelunch.uk</a> 
                | <i class="fa fa-twitter" aria-hidden="true"></i><a href="https://twitter.com/meetfreelunch" style="text-decoration: none" title="Connect with us on twitter"> @ViewBag.twitter</a>
            </p>
            <p>&copy; @DateTime.Now.Year - freelunch.uk</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
